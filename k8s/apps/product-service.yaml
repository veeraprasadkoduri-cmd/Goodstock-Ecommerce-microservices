apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: devops-ecommerce
spec:
  type: ClusterIP

  selector:
    app: product-service

  ports:
    - name: http
      port: 4001
      targetPort: 4001